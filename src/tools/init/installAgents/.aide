---
scope: src/tools/init/installAgents
status: aligned
intent: >
  Install the canonical AIDE pipeline agent files into the host project's
  `.claude/agents/` directory. Each agent maps to one pipeline phase —
  spec-writer, domain-expert, strategist, architect, implementor, QA,
  aligner, auditor — and carries its own system prompt, tool allowlist,
  model assignment, and MCP server declarations (including Obsidian MCP
  for brain access). Agents are the runtime identity of each pipeline
  phase: the slash commands orchestrate, the agents execute. Existing
  agent files in the host project are never overwritten — customizations
  survive init.
outcomes:
  desired:
    - Eight agent files are installed to `.claude/agents/aide/`:
      aide-spec-writer, aide-domain-expert, aide-strategist,
      aide-architect, aide-implementor, aide-qa, aide-aligner,
      aide-auditor. Each has YAML frontmatter (name, description, tools,
      model, mcpServers, color) and a Markdown body that serves as the
      agent's system prompt.
    - Agent content is read through the initContent chokepoint — the same
      `readCanonicalDoc()` path used by methodology docs and commands. No
      agent content is hardcoded as template literals in this module.
    - Obsidian MCP is declared in `mcpServers` for agents that need brain
      access — domain-expert (write), strategist (read), architect (read),
      auditor (read). The declaration is portable (server name reference,
      not inline config) so the host project provides its own Obsidian MCP
      wiring.
    - A generic `study-playbook` skill template is installed to
      `.claude/skills/study-playbook/SKILL.md` in the host project. It
      teaches the hub-navigation pattern against the host's own
      coding-playbook vault path. Existing skills are not overwritten.
    - Model assignments match task demands — opus for deep reasoning
      (spec-writer, strategist, architect, aligner, auditor), sonnet for
      throughput (domain-expert, implementor, QA).
    - The AIDE methodology docs specify the canonical agent list so that
      any reader of the docs knows which agents exist and which phases
      invoke them.
    - Phase commands reference agents by name (e.g., "Delegate to the
      `aide-architect` agent") instead of vague role descriptions.
    - Each step reports created/exists/skipped status, consistent with
      the init orchestrator's reporting pattern.
    - Re-running on a project with all agents present changes nothing
      and reports all as existing.
  undesired:
    - An init that overwrites a user's customized agent file because
      "the canonical version is newer." Customizations survive init;
      upgrades are aide_upgrade's job.
    - Agent prompts that reference personal vault paths, specific user
      brain locations, or non-portable MCP configurations. Agents are
      opinionated about Obsidian MCP but agnostic about where the vault
      lives.
    - Agent prompts that duplicate orchestration logic from the phase
      commands. The command says what to do; the agent knows how to do it.
    - A study-playbook skill that overwrites an existing user-defined one.
      The host may have customized their playbook navigation; init respects
      that.
    - Agents that attempt to spawn other agents. Claude Code prohibits
      subagent nesting — every agent returns results to the caller.
    - Agent content hardcoded as string literals in this module instead
      of read through initContent. That creates a second source of truth
      that drifts from the canonical agent files.
    - An init that wires Obsidian MCP inline in every agent's mcpServers
      field with full connection config. The host project should wire
      Obsidian MCP once in `.mcp.json`; agents reference it by name.
---

## Context

The AIDE pipeline currently defines eight phases (spec, research, synthesize,
plan, build, QA/fix, align, refactor), each described by a slash command that
tells the orchestrating agent what to do. But the commands have no agents to
delegate to — they say "spin up a Planner agent" without shipping one. Users
must define their own agents or rely on the orchestrator to inline the work,
which defeats the fresh-context-per-phase architecture that makes the
pipeline reliable.

Claude Code agents are `.md` files in `.claude/agents/` with YAML
frontmatter (name, description, tools, model, mcpServers, color, etc.)
and a Markdown body that becomes the agent's system prompt. Agents cannot
nest — they return results to the caller. Up to 10 can run simultaneously.
The orchestrator delegates by naming the agent in the prompt or via
`@agent-<name>` mention.

AIDE is opinionated about the knowledge layer: Obsidian MCP is the brain.
The domain expert writes to it, the strategist reads from it, the architect
and auditor read the coding playbook from it. Host projects bring their own
vault — the agents teach the workflow pattern, not our content.

## Strategy

**One agent per pipeline role, not per phase.** Eight agents map to the eight
distinct roles the pipeline needs. The implementor serves both build and
fix phases — same role, same prompt, different inputs. The domain expert and
strategist are separate agents because their brain-access patterns differ
(write-heavy vs read-heavy) and their reasoning demands differ (volume
research vs deep synthesis).

**Agents declare MCP servers by name reference, not inline config.** An
agent that needs Obsidian declares `mcpServers: [obsidian]`. The host
project's `.mcp.json` provides the actual Obsidian server config (vault
path, connection details). This keeps agents portable across vaults while
being opinionated that Obsidian is the brain layer.

**Model assignments optimize for task shape.** Opus for roles that require
deep reasoning under ambiguity (spec-writer distilling intent, strategist
synthesizing research, architect making structural decisions, aligner
resolving drift, auditor evaluating refactor targets). Sonnet for roles
that require throughput and pattern-matching (domain-expert doing volume
web research, implementor executing plans, QA doing systematic checking).

**The study-playbook skill ships as a generic template.** It teaches the
hub-navigation pattern (read index → identify sections → drill into child
notes → follow wikilinks) but points at the host's own `coding-playbook/`
vault path. The host builds their own playbook; the skill teaches them how
to consume it. The architect and auditor agents' prompts reference this
skill by name.

**Agent files flow through the initContent chokepoint.** Canonical agent
`.md` files live in the repo at `.claude/agents/`. The `initContent` module
registers them in DOC_PATHS. The `installAgents` helper reads them through
`readCanonicalDoc()` and writes them to the host's `.claude/agents/`.
Same byte-faithful copy pattern as methodology docs and commands.

**Phase commands are updated to name agents explicitly.** Each command's
delegation instruction changes from "spin up a Planner agent" to "delegate
to the `aide-spec-writer` agent" with an `@agent-aide-spec-writer` mention
pattern. The command owns the what; the agent owns the how.

## Good examples

A fresh `aide_init` run reports agents alongside everything else:

> ✓ aide-spec-writer: Created
> ✓ aide-domain-expert: Created
> ✓ aide-strategist: Created
> ✓ aide-architect: Created
> ✓ aide-implementor: Created
> ✓ aide-qa: Created
> ✓ aide-aligner: Created
> ✓ aide-auditor: Created
> ✓ study-playbook skill: Created

A re-run on the same project:

> - aide-spec-writer: Already exists
> - aide-domain-expert: Already exists
> ...

A host project that has customized `aide-architect.md` with project-specific
conventions runs init and sees the architect reported as "Already exists" —
their customizations survive.

The `/aide:plan` command says: "Delegate to the `aide-architect` agent.
Provide it the path to the `.aide` spec and let it produce `plan.aide`."
The agent's own prompt handles the rest — reading the spec, consulting the
playbook via study-playbook, scanning the codebase, writing the plan.

## Bad examples

An agent file that hardcodes `d:/Code/Me/my-brain` as the vault path.
The host project has a different vault. The agent fails on first brain
access.

An agent prompt that includes the full study-playbook navigation
instructions inline instead of referencing the skill. When the skill
template is updated, the agent's inline copy drifts.

A phase command that still says "spin up an Architect agent" without
naming `aide-architect`. The orchestrator doesn't know which agent file
to use and falls back to inlining the work in the main context.

An init that installs agents but doesn't install the study-playbook skill.
The architect agent references the skill in its prompt, the skill doesn't
exist, and the agent skips playbook consultation entirely.
