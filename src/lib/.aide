# Lib Layer — Intent Spec

## Data Pipeline

Five modules: three form the discovery pipeline (scan → classify → buildTree),
two support initialization (detectFramework, initContent).

```
scan/       → walks filesystem, returns raw AideFile[]
classify/   → classifies files by type, detects anomalies
buildTree/  → formats AideFile[] into a progressive disclosure tree string
```

Each module is independently usable. scan doesn't depend on classify.
buildTree doesn't depend on scan. They compose through the shared AideFile type.

## scan/ — Filesystem Walker

**Input:** root directory path, optional subdirectory scope
**Output:** ScanResult (root + AideFile[])

- Recursive `fs.readdir` with `{ withFileTypes: true }`
- Match files ending in `.aide`: `.aide`, `intent.aide`, `research.aide`, `todo.aide`
- Skip directories: node_modules, .git, dist, build, .next, coverage, __pycache__
- For each match: read first ~500 bytes, extract summary from first paragraph
- Normalize all paths to POSIX (forward slashes) for consistent display
- Summary extraction: first non-empty, non-heading line, truncated to ~80 chars

## classify/ — Type Detection + Anomaly Detection

### classifyFile(filename)
- `.aide` → "intent"
- `intent.aide` → "intent"
- `research.aide` → "research"
- `todo.aide` → "todo"

### detectAnomalies(files, root)
Groups files by directory and checks for:
- **naming-conflict:** `.aide` + `intent.aide` in same folder
- **orphaned-research:** `research.aide` without intent spec in same folder
- **orphaned-spec:** .aide in folder with no `index.ts` (or similar orchestrator)
- **missing-spec:** `index.ts` with 3+ relative `./` imports but no .aide file

Orchestrator detection heuristic: an `index.ts` (or `index.js`) that contains
2+ import statements from `./` subdirectories. This is a heuristic — label
results as warnings, not errors.

## buildTree/ — Progressive Disclosure Tree Builder

**Input:** AideFile[], root path
**Output:** formatted tree string

- Group files by directory path
- Collapse directories that contain no .aide files (skip intermediate empty dirs)
- Draw tree with Unicode box-drawing characters (├──, └──, │)
- Each leaf shows: `filename [type] — summary`
- Type tag in brackets: [intent], [research], [todo]
- Sort directories alphabetically, files within a directory by type then name

## detectFramework/ — Agent Framework Detection

**Input:** root path, optional framework override
**Output:** FrameworkConfig (framework type + config/command/MCP paths)

- Checks for marker files/directories in priority order: Claude → Cursor → Windsurf → Copilot
- If framework param provided, skips detection and returns config for that framework
- Defaults to Claude Code if nothing detected

## initContent/ — Template Content

Pure functions returning string templates. No filesystem operations.

- `getMethodology()` — AIDE methodology text with idempotency markers
- `getMethodologyMarker()` — the marker string for idempotency detection
- `getCommands()` — map of 5 command filenames to their checklist content

## configureIde/ — IDE File Association Setup

Two functions, one per IDE family. Both are idempotent.

### configureZed(projectRoot)
- Reads `.zed/settings.json` from project root (creates if missing)
- Merges `{ "file_types": { "Markdown": ["*.aide"] } }` into existing settings
- Preserves all other settings — only touches `file_types.Markdown`
- Returns InitStepResult with status "exists" | "created"
- **Why not an extension:** Zed's markdown preview is hardcoded to the built-in
  Markdown language. A custom language with `grammar = "markdown"` gets the
  tree-sitter parser but not the preview pane. The extension API doesn't support
  custom panels. `file_types` is the only path to rendered preview.

### configureVscode(extensionsDir)
- The `.vsix` is bundled at `extensions/vscode/aide-markdown-{version}.vsix`
- Checks if already installed: `code --list-extensions | grep aide-markdown`
- If not installed: `code --install-extension <path-to-vsix>`
- Returns InitStepResult with status "exists" | "installed" | "skipped"
- Skipped if `code` CLI is not on PATH (not an error — user may not have VS Code)
- **Why this works:** VS Code extensions can map file associations to existing
  built-in language IDs. `"id": "markdown"` with `"extensions": [".aide"]` gives
  full syntax highlighting + preview with zero grammar bundling.

## Boundary Rule

No MCP SDK types in this layer. Functions accept plain strings, return plain
objects (AideFile, ScanResult, ValidationWarning, etc.) from types/. The tools
layer is responsible for bridging between MCP request/response and lib data.
