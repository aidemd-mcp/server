# AIDE HANDOFF

Cold-start protocol for agents reorienting on this project. This is a
snapshot of the current architectural intent, the implementation gap
between intent and code, and the pipeline phase that comes next. It is
NOT historical commentary — superseded design decisions are cut, not
annotated.

---

## Cold-start protocol (do this first if you're picking up cold)

**The brain layer is fully aligned at every level.** Intent, plans,
code, and tests are all coherent across the cascading tree. The two
multi-module refactors that were in flight (brain.aide schema collapse
+ hub-artifact sourcing from brain.aide body sections) are both
complete. `aide_discover` reports no `status: misaligned` anywhere
under `src/service/`.

**What just landed (this most recent cycle, on top of the schema
collapse):** the brain layer no longer holds hub-artifact bytes as
inline TypeScript constants. The bundled `brain.aide` body grew from
one section (`## Prose`) to three (`## Prose`, `## Playbook hub`,
`## Research hub`). The install service sources playbook-hub and
research-hub artifact bytes from those sections via `parseBrainAide`,
not from `*_TEMPLATE` constants. The vault-root `CLAUDE.md` scaffold
step was retired entirely — the framework's brain contract is now
exactly two required hubs: **coding-playbook hub** + **research hub**.
A new `Research hub` artifact (at `<brain-root>/research/research.md`)
ships for the first time. Storage-specific identifiers in
`provisionBrain/index.ts` were swept (`vaultExists` →
`brainRootExists`, `VAULT_DIRS` → `BRAIN_ROOT_DIRS`,
`buildVaultStep` → `buildBrainRootStep`, etc.); the `obsidian` token
remains only where it names content (the `obsidianBrainAideTemplate`
helper folder, the `.obsidian` directory heuristic comment) or the
legacy MCP key being migrated away from.

**Tests: 824/824 passing. tsc: clean.**

If you're picking up cold, the next pipeline phase is whatever the
user wants — there's no in-flight build work waiting to land. The
small open items below are independent followups, not blockers.

---

## The architectural intent (what the specs now say)

The brain layer is governed by these decisions, all of which live in
`.aide/intent.aide` outcomes and cascade into the affected child
specs. Read them in the root before drilling into any child.

1. **`brain.aide` lives at `.aide/config/brain.aide`.** The
   `.aide/config/` directory is user-owned: scaffolded once at
   `aide_init` if absent, never overwritten by `aide_init` or
   `aide_upgrade` after that. The directory path itself is the
   ownership signal — install/upgrade tooling reads it, no per-file
   allowlist.

2. **The default scaffolded launcher is `@bitbonsai/mcpvault`.** The
   bundled `obsidianBrainAideTemplate` helper emits `npx
   @bitbonsai/mcpvault <vault-path>`. Hosts that want a different
   brain edit one file and run `npx aidemd-mcp sync`.

3. **Frontmatter schema = exactly two top-level fields:** `name` (a
   human-readable label the package never branches on) and
   `mcpServerConfig` (`command` + `args` that sync writes verbatim
   into `.mcp.json` under `mcpServers["brain"]`). Final shape:

   ```yaml
   ---
   name: obsidian
   mcpServerConfig:
     command: npx
     args:
       - "@bitbonsai/mcpvault"
       - "D:/notes/my-vault"
   ---
   ```

4. **Body schema = exactly three named sections:** `## Prose`,
   `## Playbook hub`, `## Research hub`. The closed-vocabulary parser
   rejects unknown top-level `## ` headings as `malformed-body` and
   rejects missing required sections by name. Nested headings inside
   any section MUST use `### ` or deeper — the walker treats every
   `^## .+$` line as a section-boundary candidate.

5. **Each consumer reads only its owned sections:**
   - `tools/brain` (runtime brain entry-point tool) reads `prose` only,
     returns it byte-identical to callers
   - `provisionBrain` reads `playbookHub` and `researchHub` to scaffold
     the hub artifacts on cold install
   - `cli/sync` and `buildBrainState` read frontmatter only; body
     sections are irrelevant to them

6. **Hub artifact bytes flow from `brain.aide`, never from inline
   TypeScript constants.** `PLAYBOOK_HUB_TEMPLATE` and
   `VAULT_CLAUDE_MD_TEMPLATE` are deleted; the bytes that used to
   live in those constants are now in the bundled template's
   `## Playbook hub` body section. The user can edit them like any
   other content in their `brain.aide`.

7. **The framework's brain contract = exactly two required hubs.**
   Coding-playbook hub at `coding-playbook/coding-playbook.md`,
   research hub at `research/research.md`. Anything else in the brain
   (CLAUDE.md, journal, kanban, identity) is the user's choice, not
   part of the AIDE contract. The retired vault-root CLAUDE.md is
   GONE, not deprecated — its scaffolding step, its template constant,
   and its tests are all deleted.

8. **`provisionBrain` is a 5-step pipeline, in this order:**
   (1) Brain config (brain.aide) → (2) Brain root directories →
   (3) Playbook hub → (4) Research hub → (5) Brain MCP entry. Steps
   3 and 4 source content from the brain.aide planned by step 1 via
   `parseBrainAide` / `parseBrainAideFromString`. Step 1 lives under
   `.aide/config/`; subsequent runs treat it as user-owned and never
   would-overwrite. Hub steps surface `would-skip` (with
   `instructions`) when brain.aide is malformed — no silent empty
   scaffolding.

9. **Removed frontmatter fields:** `connector`, `rootPath`,
   `entryFile`, `tools`. The parser actively rejects these (returns
   `malformed-frontmatter` naming every stale field it found) so users
   migrate cleanly rather than carrying dead fields forward.

10. **No code path branches on `name`.** It's a label for the user
    and the agent, never a dispatcher. Different brains may share
    names without affecting code behavior.

11. **No introspection of `mcpServerConfig.args` for any purpose.**
    Don't extract a vault path, don't infer connector identity, don't
    stat any derived path. Reading args for any purpose other than
    passing them through to `.mcp.json` is the same regression as a
    `rootPath` field in different clothing.

12. **Boot reporter brain status states = four:** `ok`,
    `no-brain-aide`, `no-mcp-entry`, `mcp-drift`. The prior
    `invalid-path` state is gone with `rootPath` — there is no path
    for the package to validate anymore.

13. **`${...}` interpolation of frontmatter values into
    `mcpServerConfig.args` is supported** for advanced users who want
    to DRY a value across argument positions. The default scaffold
    does NOT exercise this surface (the path is inline, byte-for-byte).
    Substitution lives ONLY in `mcpServerConfig.args` at install/sync
    time — NEVER in any body section at agent time.

14. **The MCP server never silently rewrites `.mcp.json`.** Mutation
    of the host's MCP wiring is always an explicit user action through
    `npx aidemd-mcp sync`. Drift between `brain.aide` and `.mcp.json`
    is a hard halt — the boot reporter surfaces `mcp-drift` and the
    pipeline refuses to advance until the user runs sync.

15. **Storage-agnostic identifiers in code.** No "vault", "obsidian"
    (other than the `obsidianBrainAideTemplate` helper folder name and
    legacy-key migration prose), "hub note", "wikilink", or "note"
    (used as a synonym for "file") in TypeScript identifiers,
    comments, or string constants in `provisionBrain/index.ts` and
    sibling code. Methodology guidance about how to navigate the brain
    lives in pipeline prompts, never in code constants.

16. **Strict-failure migration for body schema changes.** A
    pre-widening prose-only `brain.aide` returns `malformed-body`
    naming the missing sections; the parser does not silently default
    them. Users hand-edit their file to add the new sections. No
    `aide_upgrade` carve-out (yet — could be added later if it turns
    out users need it).

---

## The implementation gap (what the code does NOT yet do)

Everything in the brain layer is aligned. The remaining gap items are
small followups, not blockers:

| Item | Current state | Needed |
|---|---|---|
| `.aide/docs/brain-aide.md` | Documents the 2-field frontmatter and the single `## Prose` body section. | Update the body section to document the 3-section body schema (`## Prose`, `## Playbook hub`, `## Research hub`), the closed-vocabulary parser rule, and the per-consumer ownership pattern. |
| `src/cli/sync/index.ts` IIFE | Top-level auto-exec calls `process.exit(1)` on error during module import, which vitest surfaces as an "Error: 1 error" line during test runs. All tests still pass; the noise is purely test-harness teardown. | Extract the body of the IIFE into an exported function and gate the auto-exec on an `import.meta.main`-style check (or equivalent) so importing the module for testing doesn't trigger business logic. Small structural refactor; not blocking. |

---

## Anti-regression invariants the next build phase must hold

These are the hard rules from `.aide/intent.aide` outcomes.undesired
plus the post-cycle additions. Any future build phase must not violate
them, even if a quick path through a ticket looks like it could:

- **No code path branches on `name`.** `name` is metadata. If a future
  consumer wants per-brain behavior, push it back into the user's
  prose — never into a registry.
- **No introspection of `mcpServerConfig.args`.** Don't extract a
  vault path, don't infer connector identity, don't stat any derived
  path.
- **No silent acceptance of deprecated frontmatter fields.**
  `connector` / `rootPath` / `entryFile` / `tools` must be REJECTED
  with a `malformed-frontmatter` reason, not stripped on the floor.
- **No silent acceptance of unknown body section headings.** Any
  top-level `## <heading>` not in `REQUIRED_BODY_HEADINGS` returns
  `malformed-body` naming the unknown heading. The required set is
  exactly three: `## Prose`, `## Playbook hub`, `## Research hub`.
- **No silent default-filling of missing body sections.** A
  pre-widening prose-only `brain.aide` returns `malformed-body`; users
  hand-edit. No `aide_upgrade` carve-out auto-migrates body schema.
- **No server-side substitution in any body section.** All three body
  sections return verbatim. Substitution lives only in
  `mcpServerConfig.args` at install/sync time.
- **No silent `.mcp.json` mutation.** The MCP server reads `.mcp.json`;
  it never rewrites it. Sync is the only mutation surface.
- **No overwrite of files under `.aide/config/` on subsequent
  `aide_init` / `aide_upgrade` runs.** Scaffolded once if absent;
  user-owned forever after.
- **No new frontmatter field whose only consumer is the parser's own
  validation.** Every field must name a real runtime consumer.
- **No inline TypeScript constants for hub-artifact bytes.**
  `PLAYBOOK_HUB_TEMPLATE` and `VAULT_CLAUDE_MD_TEMPLATE` were deleted
  for a reason. Hub bytes flow from brain.aide body sections via
  `parseBrainAide`, full stop.
- **No CLAUDE.md scaffolding step under any name.** "Vault CLAUDE.md",
  "brain CLAUDE.md", "navigation hub" — all rejected. The framework's
  brain contract is exactly the two hubs (playbook + research). Users
  who want a CLAUDE.md add their own.
- **No backend-driver dispatch, no connector registry, no
  matcher-by-shape.** Brain identity is whatever the user wrote in
  their `brain.aide`. Adding a new brain is a user-side edit, never
  a code-level extension.
- **No "vault" / "obsidian" (outside allowed exceptions) /
  "wikilink" / "hub note" / "note" tokens in code identifiers,
  comments, or string constants in install-service modules.** The
  allowed exceptions are: the `obsidianBrainAideTemplate` helper folder
  name (describes content, not a code branch), the `.obsidian`
  directory heuristic comment in `brainRootExists` (an
  honestly-labeled implementation detail), and the legacy `obsidian`
  MCP key in migration branches (the key being migrated away from).
- **No reading another consumer's owned body section.** `tools/brain`
  must not read `playbookHub` or `researchHub`. `provisionBrain` must
  not read `prose`. Cross-section reads violate the per-consumer
  ownership contract.

---

## Working tree state

After the most recent cycle, the working tree is clean post-commit.
The package publishes via `node scripts/publish.mjs <patch|minor|major>`
which bumps versions, syncs `server.json`, tags, and lets CI handle
the npm + MCP Registry publish. Safe to publish once the methodology
doc update lands; without it, a publish would ship code and a doc that
disagree on the body schema.

---

## Resume protocol for next session

1. Run `/aide` — orchestrator boots, runs `aide_info`, reads docs +
   intent tree.
2. Read this handoff to confirm the state above is still current.
3. If picking a followup from the gap table above: that work is small
   and self-contained. Either go straight to `/aide:spec` (for the
   methodology doc) or `/aide:plan` (for the cli/sync IIFE refactor).
4. If starting a new feature: standard `/aide` flow — interview,
   spec, research-or-skip, synthesize, plan, build, qa.

If the user asks "what's the state?" — answer: brain layer fully
aligned end-to-end (intent + plans + code + tests), tests green,
two small followups open (methodology doc one revision behind on the
body schema; cli/sync top-level IIFE causes test-harness noise),
otherwise no in-flight work.
